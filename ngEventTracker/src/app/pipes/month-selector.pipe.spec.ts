import { MonthSelectorPipe } from './month-selector.pipe';

describe('MonthSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
